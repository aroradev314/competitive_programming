#include <iostream>

int main() {
	int t;
	cin >> t;
	while (t--) {
		string s;
		cin >> s;
		
		int as = 0;
		int ans = 0;
		int start = 0;
		for (int i = 1; i < n; i++) {
			if (s[i] == 'B' && s[i - 1] == 'B') {
				 
				
